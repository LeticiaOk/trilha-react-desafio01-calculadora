import { createGlobalStyle } from 'styled-components' // + Importa função de styles-components que define estilos globais para toda a aplicação, ou seja para todos os componentes.
export default createGlobalStyle` 
  *, body { 
    margin: 0;
    padding: 0;
  }
`// + define estilos globais para toda a aplicação.